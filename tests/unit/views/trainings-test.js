import { moduleFor, test } from 'ember-qunit';

moduleFor('view:trainings', 'Unit | View | trainings');

// Replace this with your real tests.
test('it exists', function(assert) {
  let view = this.subject();
  assert.ok(view);
});
